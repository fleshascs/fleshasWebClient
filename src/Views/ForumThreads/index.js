import React, { Component } from "react";
import { ForumThreads } from "../../components";

class ForumThreadsView extends Component {
  render() {
    return (
      <div className="container mt-5">
        <ForumThreads />
      </div>
    );
  }
}

export default ForumThreadsView;
